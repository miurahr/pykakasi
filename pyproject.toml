[build-system]
requires = ["setuptools>=45.3.0", "wheel", "setuptools_scm[toml]>=3.5.0", "klepto", "six"]
build-backend = "setuptools.build_meta"

[tools.setuptools_scm]
local_scheme = "no-local-version"
